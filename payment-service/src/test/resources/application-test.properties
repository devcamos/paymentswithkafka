# Test Configuration
spring.application.name=demo-test

# Server Configuration
server.port=0

# Kafka Configuration (disabled for unit tests)
spring.kafka.bootstrap-servers=
spring.kafka.consumer.group-id=test-payment-group
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.apache.kafka.common.serialization.StringSerializer

# Topic Configuration
kafka.topic.payments=test-payments
kafka.topic.payment-events=test-payment-events

# WebSocket Configuration
spring.websocket.enabled=false

# Logging
logging.level.com.example.demo=DEBUG
logging.level.org.springframework.kafka=WARN
